interface:
  display_name: Domain 実装
  short_description: model / value object / UseCase / Repository interface / provider を扱う。
  default_prompt: Domain 層の変更対象の役割クラスを判定し、該当 reference に沿って実装する。
policy:
  allow_implicit_invocation: true
