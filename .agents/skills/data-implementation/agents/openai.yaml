interface:
  display_name: Data 実装
  short_description: Repository 実装 / mapper / DataStore / export-import / ContentResolver を扱う。
  default_prompt: Data 層の変更対象の役割クラスを判定し、該当 reference に沿って実装する。
policy:
  allow_implicit_invocation: true
